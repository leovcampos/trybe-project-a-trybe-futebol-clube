import User from "../../database/models/User"

const userMock = {
    id: 1,
    username: 'user example',
    password: 'xablaupass',
    role: 'admin',
    email: 'user@email.com'
}

export default { 
    userMock 
}
